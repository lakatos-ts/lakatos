# npm owns this file's formatting
package-lock.json
engines/thales/package-lock.json

# local Claude Code artifacts (globally gitignored, so absent in CI)
.claude/

# the two Lean packages' build trees and lake-managed manifests
engines/thales/.lake/
engines/thales/lake-manifest.json
tarski/.lake/
tarski/lake-manifest.json

# hand-curated release notes
engines/thales/CHANGELOG.md

# the Lemma spec's fixtures are conformance inputs, not repo prose
lemma/spec/

# bd rewrites its own config and metadata
.beads/

# the evaluator's fixtures: parser inputs kept in the shape they test, and
# goldens that are the bridge's output byte for byte
tarski/frontend/tests/fixtures/
tarski/Test/Tarski/fixtures/

# the pinned test262 clone: upstream's bytes, and gitignored one directory
# down, which prettier does not read
tarski/.test262/
